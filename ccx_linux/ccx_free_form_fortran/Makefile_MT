
DIR=../SPOOLES.2.2

CFLAGS = -Wall -O3 -fopenmp -I $(DIR) -DARCH="Linux" -DSPOOLES -DARPACK -DMATRIXSTORAGE -DUSE_MT=1
FFLAGS = -Wall -O3 -fopenmp -ffree-form

CC=cc
FC=gfortran

.c.o :
	$(CC) $(CFLAGS) -c $<
.f.o :
	$(FC) $(FFLAGS) -c $<

include Makefile.inc

SCCXMAIN = ccx_2.15.c

OCCXF = $(SCCXF:.f=.o)
OCCXC = $(SCCXC:.c=.o)
OCCXMAIN = $(SCCXMAIN:.c=.o)

LIBS = \
       $(DIR)/MT/src/spoolesMT.a \
       $(DIR)/spooles.a \
       ../ARPACK/libarpack_INTEL.a \
       -lpthread -lm

ccx_2.15_MT: $(OCCXMAIN) ccx_2.15_MT.a  $(LIBS)
	./date.pl; $(CC) $(CFLAGS) -c ccx_2.15.c; $(FC) -fopenmp -Wall -O3 -o $@ $(OCCXMAIN) ccx_2.15_MT.a $(LIBS)

ccx_2.15_MT.a: $(OCCXF) $(OCCXC)
	ar vr $@ $?
                                                                               
